apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: istio-spiffe-reconciler-scan
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: istio-spiffe-reconciler-scan
subjects:
  - kind: ServiceAccount
    name: istio-spiffe-reconciler
    namespace: PLACEHOLDER_NAMESPACE
